class PushNotification {
  String? title, body, threadId, message, id, member, createdAt;
  PushNotification(
      {this.title,
      this.body,
      this.threadId,
      this.message,
      this.id,
      this.member,
      this.createdAt});
}
